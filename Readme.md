# Backend
# npm init -y 
# npm i express dotenv body-parser cors mongoose
express = main framework for buliding webserver and APIS in node.js

dotenv = loads environment vaibable form a .env file into process.env

body-parser = parses the incoming request body
Express doesn’t handle request bodies by default, so body-parser helps convert raw request data into a readable JavaScript object.

cors = allows frontend and backend to make API requests

moongoose = mongodb object modeling tool for Node.js

